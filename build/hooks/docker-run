#!/usr/bin/env bash
set -e

# This should be run as root, from the project root directory

# If you pass in configs that require different ports, update this. 

docker run -d \
        -p 8080:8080 \
        --name frms-logstash-56 \
        frms-logstash-56
